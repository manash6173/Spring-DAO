
  1  CREATE OR REPLACE PROCEDURE P_AUTHENTICATE(USER IN VARCHAR2,PASS IN VARCHAR2,RESULT OUT VARCHAR2)
  2  AS
  3    CNT NUMBER(2);
  4  BEGIN
  5    SELECT COUNT(*) INTO CNT FROM USERLIST WHERE USERN=USER AND PWD=PASS;
  6    IF(CNT<>0) THEN
  7      RESULT:='VALID CREDIENTIALS';
  8    ELSE
  9     RESULT:='INVALID CREDIENTIALS';
 10    END IF;
 11* END;
SQL> /

Procedure created.